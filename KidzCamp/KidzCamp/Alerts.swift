//
//  Alerts.swift
//  KidzCamp
//
//  Created by Alex Cowley  on 6/3/19.
//  Copyright © 2019 Alex Cowley . All rights reserved.
//
/*
import Foundation
import UIKit

class Alerts {
    
    
    static func showBasicAlert(on vc:UIViewController, with title: String, message: String) {
        let alert = UIAlertController(title: title, message: message, preferredStyle: .alert)
        alert.addAction(UIAlertAction(title: "Continue", style: .default, handler: { (UIAlertAction) in
            let layout = UICollectionViewFlowLayout()
            let nextView = CollectionViewController(collectionViewLayout: layout)
            
        }))
        vc.present(alert, animated: true)
    }
    
    static func showContinueAlert(on vc: UIViewController) {
        showBasicAlert(on: vc, with: "Let's get started!", message: "Please select continue to begin using Kidz Camp.")
        showBasicAlert(on: vc, with: "ddd", message: "eee")
    }
    
    
}

class ModifiedAlerts: Alerts {
    
    override func showBasicAlert(on vc:UIViewController, with title: String, message: String) {
        
    }
}
*/
